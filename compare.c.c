#include <stdio.h>
int main(void)
{
	float a,b;
	printf("Dose duo dekadikous arithmous:");
	scanf("%f %f",&a,&b);
	
	if (a>b)
		{
			printf("%f",b);
		}
	if (a<b)
		{
			printf("%f",a);
		}
	return 0;
		
}
